<% provide(:title, 'サインアップ') %>
<h1>サインアップ</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "登録", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
